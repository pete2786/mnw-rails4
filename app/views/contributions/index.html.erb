<h1><%=app_name%> Leaderboard</h1>
<p>Want to climb the leaderboard? Read about <%=link_to "our reputation system", reputation_path%> to learn how to earn points and badges.</p>
<div class="row">
  <div class="col-lg-6">
    <h2>Top Phrases</h2>
    <table class="table table-striped table-hover table-condensed">
      <thead>
        <th></th>
        <th>Phrase</th>
        <th>Conditions</th>
        <th>+/-</th>
      </thead>
      <tbody>
        <% @contributions.top_phrases.each do |p| %>
        <tr>
          <td><%=image_tag p.thumb, height: 25, width: 25%></td>
          <td><%=link_to truncate(p.phrase, limit: 25), phrase_path(p)%></td>
          <td><%=p.condition%>/<%=p.temperature%></td>
          <td><%=p.phrase_vote_rep%></td>
        </tr>
        <% end %>
      </tbody>
    </table>
    <p><%=link_to "Browse all phrases", phrases_path%>
  </div>

  <div class="col-lg-6">
    <h2>Point Leaders</h2>
    <table class="table table-striped table-hover table-condensed">
      <thead>
        <th>User</th>
        <th>Points</th>
      </thead>
      <tbody>
        <% @contributions.top_users.each do |u| %>
        <tr>
          <td><%=image_tag u.image, height: 25, width: 25%> <%=u.name%></td>
          <td><%=u.points%></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>

<div class="row">
  <div class="col-lg-6">
    <h2>Top Contributers</h2>
    <table class="table table-striped table-hover table-condensed">
      <thead>
        <th>User</th>
        <th>Phrase Submissions</th>
      </thead>
      <tbody>
        <% @contributions.top_contributers.each do |u| %>
        <tr>
          <td><%=image_tag u.image, height: 25, width: 25%> <%=u.name%></td>
          <td><%=u.phrases.count%></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>

<hr />
